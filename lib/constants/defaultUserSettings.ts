import { UserPageSettings } from '../../entities/UserPageSettings';

export const DEFAULT_USER_SETTINGS: Partial<UserPageSettings> = {
    enableTimeline: true,
    enableExperiences: true,
    enableGithub: true,
};
